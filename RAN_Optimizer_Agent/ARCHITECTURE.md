# System Architecture - RAN Optimizer

## High-Level Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│                    RAN NETWORK OPTIMIZER                        │
│                  (Autonomous AI Agent System)                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                    ┌─────────┴─────────┐
                    │                   │
            ┌───────▼───────┐   ┌──────▼──────┐
            │  ENVIRONMENT  │   │    AGENT    │
            │   (Network)   │   │ (AI Brain)  │
            └───────┬───────┘   └──────┬──────┘
                    │                   │
                    └─────────┬─────────┘
                              │
                    ┌─────────▼─────────┐
                    │   TRAINING &      │
                    │   OPTIMIZATION    │
                    └───────────────────┘
```

## Detailed Component Architecture

### 1. RAN Environment (ran_environment.py)

```
┌──────────────────────────────────────────────────────────┐
│              RAN Network Simulator                       │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌────────┐  ┌────────┐  ┌────────┐      ┌────────┐   │
│  │Cell 0  │  │Cell 1  │  │Cell 2  │ ...  │Cell N  │   │
│  ├────────┤  ├────────┤  ├────────┤      ├────────┤   │
│  │Power   │  │Power   │  │Power   │      │Power   │   │
│  │Tilt    │  │Tilt    │  │Tilt    │      │Tilt    │   │
│  │HO Thr  │  │HO Thr  │  │HO Thr  │      │HO Thr  │   │
│  │Users   │  │Users   │  │Users   │      │Users   │   │
│  │TPut    │  │TPut    │  │TPut    │      │TPut    │   │
│  │Drop%   │  │Drop%   │  │Drop%   │      │Drop%   │   │
│  └────────┘  └────────┘  └────────┘      └────────┘   │
│                                                          │
│  State Vector (50 dims for 10 cells):                   │
│  [users₀, tput₀, drop₀, power₀, interf₀,               │
│   users₁, tput₁, drop₁, power₁, interf₁, ...]          │
│                                                          │
│  Action Space (27 actions):                             │
│  Power: [-3, 0, +3] dB                                  │
│  Tilt:  [-2, 0, +2] degrees                             │
│  HO:    [-5, 0, +5] threshold                           │
│  → 3 × 3 × 3 = 27 combinations                          │
│                                                          │
│  Reward Calculation:                                     │
│  + Throughput improvement                               │
│  + Drop rate reduction                                  │
│  + User satisfaction increase                           │
│  + Power savings                                        │
│  - Performance degradation                              │
└──────────────────────────────────────────────────────────┘
```

### 2. AI Agent (agent.py)

```
┌──────────────────────────────────────────────────────────┐
│          Deep Q-Network (DQN) Agent                      │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌────────────────────────────────────────────────┐     │
│  │           Q-Network (Neural Network)           │     │
│  │                                                │     │
│  │  Input(50) → FC(128) → ReLU → FC(128) → ReLU  │     │
│  │           → FC(64) → ReLU → Output(27)        │     │
│  │                                                │     │
│  │  Predicts: Q(s,a) for each action a           │     │
│  └────────────────────────────────────────────────┘     │
│                                                          │
│  ┌────────────────────────────────────────────────┐     │
│  │         Target Network (Stable Targets)        │     │
│  │  Same architecture, updated every N episodes   │     │
│  └────────────────────────────────────────────────┘     │
│                                                          │
│  ┌────────────────────────────────────────────────┐     │
│  │    Experience Replay Buffer (10,000 max)      │     │
│  │  Stores: (state, action, reward, next_state)  │     │
│  │  Samples random batches for training           │     │
│  └────────────────────────────────────────────────┘     │
│                                                          │
│  ┌────────────────────────────────────────────────┐     │
│  │         Epsilon-Greedy Policy                  │     │
│  │  Exploration: Random action (ε)                │     │
│  │  Exploitation: Best action (1-ε)               │     │
│  │  ε decays: 1.0 → 0.01 over time                │     │
│  └────────────────────────────────────────────────┘     │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

### 3. Training Loop (train_agent.py)

```
┌──────────────────────────────────────────────────────────┐
│                  Training Pipeline                        │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  FOR each episode (1 to 100):                           │
│    │                                                     │
│    1. Reset environment                                 │
│    │   state = env.reset()                              │
│    │                                                     │
│    2. Episode Loop (until done):                        │
│    │   ┌─────────────────────────────────────┐         │
│    │   │ • Agent observes state              │         │
│    │   │ • Agent selects action (ε-greedy)   │         │
│    │   │ • Environment executes action        │         │
│    │   │ • Environment returns:               │         │
│    │   │   - next_state                       │         │
│    │   │   - reward                           │         │
│    │   │   - done flag                        │         │
│    │   │ • Store experience in memory         │         │
│    │   │ • Sample batch from memory           │         │
│    │   │ • Train Q-network on batch           │         │
│    │   │ • Update epsilon (decay)             │         │
│    │   └─────────────────────────────────────┘         │
│    │                                                     │
│    3. Update target network (every 10 episodes)         │
│    │                                                     │
│    4. Log metrics & save progress                       │
│    │                                                     │
│  END                                                     │
│                                                          │
│  5. Evaluate final performance                          │
│  6. Generate visualizations                             │
│  7. Run A/B testing                                     │
│  8. Save trained model                                  │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

### 4. A/B Testing System (ab_testing.py)

```
┌──────────────────────────────────────────────────────────┐
│              A/B Testing Framework                        │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  Network Split:                                          │
│  ┌─────────────────────┐   ┌─────────────────────┐     │
│  │    Group A (Test)   │   │  Group B (Control)  │     │
│  │   With Agent Opt    │   │    No Changes       │     │
│  ├─────────────────────┤   ├─────────────────────┤     │
│  │ Cell 0, 2, 4, 6, 8  │   │ Cell 1, 3, 5, 7, 9  │     │
│  │                     │   │                     │     │
│  │ Agent applies       │   │ Baseline            │     │
│  │ optimizations       │   │ performance         │     │
│  └─────────────────────┘   └─────────────────────┘     │
│           │                         │                    │
│           └────────┬────────────────┘                    │
│                    │                                     │
│         ┌──────────▼──────────┐                         │
│         │  Measure & Compare  │                         │
│         │  - Throughput       │                         │
│         │  - Drop rate        │                         │
│         │  - Satisfaction     │                         │
│         │  - Power usage      │                         │
│         └──────────┬──────────┘                         │
│                    │                                     │
│         ┌──────────▼──────────┐                         │
│         │ Statistical Test    │                         │
│         │ Is improvement      │                         │
│         │ significant?        │                         │
│         └──────────┬──────────┘                         │
│                    │                                     │
│         ┌──────────▼──────────┐                         │
│         │   Recommendation    │                         │
│         │ ✅ Deploy to all    │                         │
│         │ ⚠️  Deploy w/caution│                         │
│         │ ❌ Do not deploy    │                         │
│         └─────────────────────┘                         │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

### 5. Dashboard (dashboard.py)

```
┌──────────────────────────────────────────────────────────┐
│            Interactive Web Dashboard                      │
│                 (Dash + Plotly)                          │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌────────────────────────────────────────────────┐     │
│  │              KPI Cards (Real-time)             │     │
│  │  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐      │     │
│  │  │ 72.5 │  │ 3.2% │  │ 83.1 │  │ 380W │      │     │
│  │  │ Mbps │  │ Drop │  │ Satis│  │Power │      │     │
│  │  └──────┘  └──────┘  └──────┘  └──────┘      │     │
│  └────────────────────────────────────────────────┘     │
│                                                          │
│  ┌──────────────────┐  ┌──────────────────┐            │
│  │ Throughput Graph │  │ Drop Rate Graph  │            │
│  │   📈 Time-series│  │   📉 Time-series │            │
│  │   Last 50 points│  │   Last 50 points │            │
│  └──────────────────┘  └──────────────────┘            │
│                                                          │
│  ┌──────────────────┐  ┌──────────────────┐            │
│  │ Satisfaction     │  │ Power by Cell    │            │
│  │   🎯 Gauge 0-100│  │   📊 Bar chart   │            │
│  └──────────────────┘  └──────────────────┘            │
│                                                          │
│  ┌────────────────────────────────────────────────┐     │
│  │         Cell Details Table                     │     │
│  │ Cell │ Users │ Tput │ Drop% │ Power │ Tilt   │     │
│  │   0  │  245  │ 68.3 │  3.8  │ 38.5  │  4.2°  │     │
│  │   1  │  312  │ 71.2 │  2.9  │ 41.0  │  3.8°  │     │
│  │  ... │  ...  │ ...  │  ...  │  ...  │  ...   │     │
│  └────────────────────────────────────────────────┘     │
│                                                          │
│  Auto-refresh: Every 2 seconds                          │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

## Data Flow

### Forward Pass (Inference)

```
Network State
     │
     ▼
┌─────────────┐
│ Observation │  (50-dim vector: 5 metrics × 10 cells)
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Q-Network   │  Neural network forward pass
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Q-Values    │  (27 values: one per action)
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ argmax()    │  Select action with highest Q-value
└──────┬──────┘
       │
       ▼
   Action
     │
     ▼
Environment
     │
     ▼
New State + Reward
```

### Backward Pass (Training)

```
Experience Batch (64 samples)
     │
     ▼
┌─────────────────────────┐
│ Current Q-Values        │  Q(s,a) from Q-network
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Target Q-Values         │  r + γ·max Q(s',a')
│                         │  from target network
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Loss = MSE(current,     │  Mean squared error
│            target)       │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Backpropagation         │  Update network weights
└────────┬────────────────┘
         │
         ▼
    Updated Weights
```

## File Dependencies

```
train_agent.py
    ├── imports ran_environment.py
    │   └── uses gym.Env
    │
    ├── imports agent.py
    │   ├── uses torch.nn
    │   └── uses collections.deque
    │
    ├── imports ab_testing.py
    │   └── uses dataclasses
    │
    └── uses matplotlib (for plotting)

dashboard.py
    ├── imports ran_environment.py
    │
    ├── imports agent.py
    │
    ├── uses dash
    │
    └── uses plotly
```

## Performance Characteristics

### Time Complexity

| Operation | Complexity | Typical Time |
|-----------|-----------|--------------|
| Agent forward pass | O(n) | <1ms |
| Agent training step | O(batch_size) | ~10ms |
| Environment step | O(num_cells) | <1ms |
| Full episode | O(steps × cells) | ~100ms |
| Full training (100 ep) | O(episodes × steps) | 5-10 min |

### Space Complexity

| Component | Memory Usage |
|-----------|-------------|
| Q-Network | ~1 MB |
| Replay Buffer (10k) | ~20 MB |
| Environment State | <1 MB |
| Training History | ~5 MB |
| **Total** | **~30 MB** |

## Scalability Analysis

### Cells vs Training Time

| # Cells | State Dims | Training Time (100 ep) |
|---------|-----------|------------------------|
| 5       | 25        | ~3 minutes            |
| 10      | 50        | ~7 minutes            |
| 20      | 100       | ~15 minutes           |
| 50      | 250       | ~40 minutes (GPU rec) |
| 100     | 500       | ~90 minutes (GPU req) |

### Actions vs Complexity

Current: 3×3×3 = 27 actions

Potential expansion:
- Add frequency selection: 3×3×3×5 = 135 actions
- Add beamforming: 3×3×3×8 = 216 actions
- Add power control granularity: 5×5×5 = 125 actions

## Integration Points

### Future Extensions

```
┌─────────────────────────────────────────────┐
│         Current System (Standalone)         │
└──────────────────┬──────────────────────────┘
                   │
    ┌──────────────┼──────────────┐
    │              │              │
    ▼              ▼              ▼
┌────────┐  ┌─────────────┐  ┌────────┐
│Network │  │  Analytics  │  │External│
│  APIs  │  │  Platform   │  │  Data  │
│Ericsson│  │  (Grafana)  │  │Weather │
│ Nokia  │  │             │  │Traffic │
│Huawei │  │             │  │ Events │
└────────┘  └─────────────┘  └────────┘
```

## Summary

This architecture implements a **complete autonomous optimization system** with:

1. ✅ Realistic network simulation
2. ✅ State-of-the-art AI agent (DQN)
3. ✅ Safe change validation (A/B testing)
4. ✅ Real-time monitoring (Dashboard)
5. ✅ Production-ready code structure

The system is **modular, extensible, and well-documented** - ready for further development or deployment!
